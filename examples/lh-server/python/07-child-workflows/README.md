# Python Child Workflows Example

Registers the documented `greeting-child` and `greeting-parent` WfSpecs.

From the repository root, complete the [shared Python setup](../README.md), then run:

```bash
python examples/lh-server/python/07-child-workflows/main.py register
python examples/lh-server/python/07-child-workflows/main.py workers
```