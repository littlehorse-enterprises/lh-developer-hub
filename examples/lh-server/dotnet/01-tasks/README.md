# .NET Tasks Example

Registers and runs the documented `greet` task worker.

```bash
dotnet run --project examples/lh-server/dotnet/01-tasks -- register
dotnet run --project examples/lh-server/dotnet/01-tasks -- workers
```