# .NET Variables Example

Fetches a user object, extracts JSON fields, formats a message, and sends an email.

```bash
dotnet run --project examples/lh-server/dotnet/03-variables -- register
dotnet run --project examples/lh-server/dotnet/03-variables -- workers
lhctl run variables-example user-id obiwan
```