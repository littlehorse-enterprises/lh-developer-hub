# .NET Conditionals Example

Chooses a message task based on a user's preferred contact method.

```bash
dotnet run --project examples/lh-server/dotnet/04-conditionals -- register
dotnet run --project examples/lh-server/dotnet/04-conditionals -- workers
lhctl run send-message user-id obiwan message hello
```