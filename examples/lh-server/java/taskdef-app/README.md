# TaskDef App

This example registers the greet TaskDef with the LittleHorse server.

## Prerequisites
- Java 21+

## Run
From the repository root:

```sh
./gradlew :examples:lh-server:java:taskdef-app:run
```

From this directory:

```sh
../../../../gradlew :examples:lh-server:java:taskdef-app:run
```
